<!-- <div class='row'>
  <a href="#" class="button" id="zip-code-button">Find a show near you!</a>
</div> -->

<div id="top-section">
  <div class="row">
    <div class='small-12 medium-4 columns' id="zip-code-form">
        <form action='/shows' method='get'>
          <label>Zip code<input type="text" id="user_zip" name="zip_code" /></label>
          <label>Distance<input type="text" id="distance" name="radius" /></label>
          <label>Genre<input type="text" id="genre" name="genre" /></label>
          <input type='submit' value='Search' class="button">
        </form>
    </div>
  </div>
</div>

<% if @shows.nil? || @shows.empty? %>
  <div class='row'>
    <div class='flash-notice small-12 medium-3 columns'>
      No shows found
    </div>
  </div>
<% end %>

<% if @shows %>
  <div id="page-buttons">
    <%= paginate @shows %>
  </div>
<% end %>

<% if @shows %>
  <div class="row">
    <% if signed_in? %>
      <%= render "rsvps" %>
    <% end %>
    <div id="shows">
      <div class="page small-12 medium-8 columns">
        <h3>Upcoming Shows</h3>
        <%= render @shows %>
      </div>
    </div>
  </div>
<% end %>
