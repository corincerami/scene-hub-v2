<div class='row show panel'>
    <a href="/shows/<%= show.id %>">
      <div class="page small-12 medium-6 columns">
        <div id="show-info">
          Bands:
          <% show.bands.each do |band| %>
            <%= band.name %>
          <% end%>
          <div>
            <%= show.venue.name %>
          </div>
          <div>
            <%= show.venue.street_1 %>
          </div>
          <div>
            <%= show.venue.street_2 %>
          </div>
          <div>
            <%= "#{show.venue.city}, #{show.venue.state} #{show.venue.zip_code}" %>
          </div>
          <div>
            <%= show.date_and_time %>
          </div>
        </div>
      </div>
      <div class="page small-12 medium-4 columns">
        <div>
          <% if show.bands.first.photos.any? %>
            <%= image_tag show.bands.first.photos.first.image.url(:thumb) %>
          <% end %>
        </div>
      </div>
    </a>
</div>
